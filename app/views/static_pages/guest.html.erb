<p id="notice"><%= notice %></p>

<h1>Goście</h1>

<table>
  <thead>
    <tr>
      <th>Imie</th>
      <th>Nazwisko</th>
      <th>Numer</th>
      <th>Osobatow</th>
      <th colspan="3">Opcje/th>
    </tr>
  </thead>

  <tbody>
    <% @guests.each do |guest| %>
      <tr>
        <td><%= guest.imie %></td>
        <td><%= guest.nazwisko %></td>
        <td><%= guest.numer %></td>
        <td><%= guest.osobatow %></td>
        <td><%= link_to 'Show', guest %></td>
        <td><%= link_to 'Edit', edit_guest_path(guest) %></td>
        <td><%= link_to 'Destroy', guest, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Guest', new_guest_path %>


<!-- <% provide(:title, "Goście") %>
<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
  </head>
  <body>
    <p>.</p>
      Tutaj znajdują się informacje na temat gości.
  </body>
</html>
-->